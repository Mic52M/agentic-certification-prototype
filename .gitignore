# Secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.Python

# Traces are runtime output; keep the dir but not the JSONL files.
traces/*.jsonl

# Experiment output (metadati, JSONL per-run, aggregati) — runtime.
experiments/*.json
experiments/exp_*/

# OS
.DS_Store

# Local-only handover / strategy docs (not pushed).
PROJECT_KNOWLEDGE.md
CERTIFICATION_MAP.md

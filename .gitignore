# Secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.Python

# Traces are runtime output; keep the dir but not the JSONL files.
traces/*.jsonl

# Experiment summaries are runtime output.
experiments/*.json

# OS
.DS_Store

# Local-only handover / strategy docs (not pushed).
PROJECT_KNOWLEDGE.md
CERTIFICATION_MAP.md
